/*import React from 'react';
import { shallow, mount } from 'enzyme';
import { beforeEach, describe, it } from 'mocha';

import loadDOM from './loadDom';

describe('AoiMap', () => {
    beforeEach((done) => {
        loadDOM(done);
        console.log('done');
    });
    it('Test prop: onChange', () => {
        const AoiMap = require('../../../components/business/AoiMap').default;
    });
});*/